#pragma once
#include "idGameSystemLocalManager.h"


//! this is nice but the data in idPlayerMetrics are encrypted in some way so i don't think i want to get into that.
class idPlayerMetricsManager {
private:

public:

	static void debugPrintAddr();

};
